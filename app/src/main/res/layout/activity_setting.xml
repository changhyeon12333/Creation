<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    tools:context=".SettingActivity">

    <ImageButton
        android:id="@+id/count_check_btn"
        android:layout_width="290dp"
        android:layout_height="45dp"
        android:layout_marginTop="200dp"
        android:src="@drawable/count_check_btn"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageButton
    android:id="@+id/priv_cng_btn"
    android:layout_width="290dp"
    android:layout_height="45dp"
    android:layout_marginTop="30dp"
    android:layout_marginBottom="20dp"
    android:src="@drawable/information_change_btn"
    app:layout_constraintEnd_toEndOf="@+id/count_check_btn"
    app:layout_constraintHorizontal_bias="0.0"
    app:layout_constraintStart_toStartOf="@+id/count_check_btn"
    app:layout_constraintTop_toBottomOf="@+id/count_check_btn" />

    <ImageButton
        android:id="@+id/app_down_btn"
        android:layout_width="290dp"
        android:layout_height="45dp"
        android:layout_marginTop="30dp"
        android:layout_marginBottom="20dp"
        android:src="@drawable/app_down_btn"
        app:layout_constraintEnd_toEndOf="@+id/priv_cng_btn"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="@+id/priv_cng_btn"
        app:layout_constraintTop_toBottomOf="@+id/priv_cng_btn" />

</androidx.constraintlayout.widget.ConstraintLayout>